# P-19B review-30 — detailed state machine

## Route and design contract

- **Intent / scope:** change one P-19B identity (`state-machine`), implementation review candidate.
- **Audience / job:** owner can read the complete lifecycle, the focal live state and the exceptional return path in one scan.
- **Reference use:** the supplied image is a structural rubric only. Scenario, Vietnamese prose, semantic IDs, geometry, CSS and SVG are independently authored.
- **Visual thesis:** a quiet P-18 state rail with one coral live state; straight centered transitions carry the normal lifecycle while one rounded dashed return route is visibly exceptional.
- **Preserve:** P-18 review-17 tokens and typography roles, three-mode semantic-role derivation, D-105 connector policy, 29 other P-19 identities, frozen P-17/P-19A/P-18.
- **Extend:** four state cards, initial/terminal markers, five straight transitions, one return transition, exact alternative table.
- **Accessibility:** state and transition meaning is present in text and the exact table; coral emphasis is repeated by labels, state code and line style.
- **Verification:** exact review-29 archive, three-mode geometry equality, centered attachments, straight-first routes, non-target preservation, neutral-light raster inspection, focused/static/regression checks.

## Observable decisions

1. Four stable-state cards use thin P-18 borders; `Đang hiệu lực` alone receives coral fill/border.
2. Every single connector attaches to the geometric center of its card edge.
3. Five normal transitions are straight; only `TRẢ LẠI · CHỈNH SỬA` uses a rounded orthogonal dashed route to avoid the forward rail.
4. Direct action labels sit on canvas-colored knockouts so no connector crosses text.
5. Initial and terminal markers are visually distinct and also represented in the semantic IR.

## Boundary

This is owner-review material, not owner approval. Browser execution remains `BLOCKED_URL_POLICY`; local deterministic SVG raster inspection is retained. P-19C, G-04, package, `dist`, publication, Git and Release are not performed.
