# P-19B review-37 — detailed five-shape process

**Authority:** D-117, retaining D-084–D-116 scope  
**Candidate:** `P19B-P18-INHERITED-THREE-MODE-REVIEW-37-1.5.0`  
**Boundary:** QA-only P-19B gallery; owner approval pending; P-19C and release work remain unauthorized.

## Outcome contract

The `process` specimen keeps the existing P-18-derived page/template, three semantic modes, typography, warm-paper/coral palette, thin-stroke hierarchy, footer legend and D-105 connector policy. Only the `process` fixture and artwork are replaced.

The independent access-approval scenario contains exactly:

- 3 terminators;
- 4 ordinary process boxes;
- 2 decision diamonds;
- 1 single-document shape with a real wave boundary;
- 1 multiple-document stack with two visible backing layers;
- 11 directed straight routes and four direct branch labels.

Every single route binds to the geometric center of its owning edge. The two routes entering the multiple-document node bind at `x=920` and `x=1080`, evenly distributed around the node center `x=1000`. No rounded-orthogonal exception is needed in this fixture.

## Reference handling

The owner image is treated only as a non-executable structural rubric for terminator/process/decision/document/multiple-document semantics and branching. The Vietnamese scenario, labels, IDs, layout, coordinates, CSS and SVG are independently authored. The existing approved diagram template is preserved rather than copied or replaced from the reference.

## QA boundary

- Archive exact review-36 before mutation.
- Permit artwork changes only in three `type-process` HTML files and one `type-process.svg` preview.
- Require 90 non-target HTML files to remain identical after candidate-ID normalization and 30 non-target previews to remain byte-identical.
- Require all 93 P-19 SVGs to retain the D-105 policy declaration.
- Require identical geometry across `neutral-light`, `neutral-dark` and `editorial`.
