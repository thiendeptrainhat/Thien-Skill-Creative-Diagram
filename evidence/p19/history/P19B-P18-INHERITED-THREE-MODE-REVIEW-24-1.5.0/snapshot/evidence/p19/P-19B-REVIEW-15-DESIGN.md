# P-19B review-15 — Layers presentation variant

**Candidate:** `P19B-P18-INHERITED-THREE-MODE-REVIEW-15-1.5.0`  
**Authority:** D-095  
**Parent:** canonical `layer-stack`  
**Status:** in-progress / owner-review-pending

## Design premise

Give a mixed technical audience one calm, immediately scannable view of a platform's abstraction levels, while making the operationally differentiating layer unmistakable without introducing a new visual system.

Creativity level is conservative-distinctive: preserve the approved P-18 warm-paper/navy/coral grammar and use only one signature element—the five-band stack with a vertical abstraction axis.

## Reference translation

The supplied image is non-executable reference data. Transferable principles are: equal horizontal bands, explicit level IDs, upward abstraction direction, direct scope labels, and one focal band. Product names, English copy, exact coordinates, CSS, SVG, template and assets are not copied.

## Implementation contract

- Add identity `layers` as a P-19B presentation variant under frozen canonical parent `layer-stack`; do not alter the 39-type taxonomy, P-17 grammar or P-19A adapter registry.
- Exactly five contiguous bands in visual order L5, L4, L3, L2, L1.
- Each band carries a direct level, title and scope label derived from semantic lanes and their owned nodes.
- Show one vertical abstraction axis from `NỀN TẢNG` toward `TRỪU TƯỢNG`.
- Exactly one focal layer: `Điều phối quy trình`. Encode it with coral boundary/fill plus visible `TRỌNG TÂM` tag and explanatory note so meaning is not color-only.
- Use original Vietnamese scenario content and a native alternative table with all five layers.
- `neutral-light`, `neutral-dark` and `editorial` may change semantic token values only; serialized SVG geometry must remain identical after mode-ID normalization.
- Preserve all 87 prior P-19 HTML artworks after candidate-ID normalization and all 29 prior neutral-light previews byte-identically; add exactly 3 Layers HTML and one preview.

## Verification

Exact checks cover parent binding, five-layer ownership/order, contiguous geometry, one focal layer, non-color redundancy, abstraction axis, alternative table, three-mode invariance, archive/protected hashes and prior-artwork preservation. Real neutral-light SVG must be rasterized and visually inspected before handoff; browser execution remains honestly blocked if the local-file policy prevents it.

P-19C, G-04 evaluation, package, `dist`, publication mirror, Git and Release remain outside authority.
