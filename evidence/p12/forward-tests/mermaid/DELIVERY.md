# Ghi chú bàn giao

- Artifact: `quy-trinh-xu-ly-ho-so.svg`
- SHA-256: `886e7363e8448b50df46f587dab1df707b22a31dbb58778233d325ab09179375`
- Quan hệ giữ nguyên: `A → B`, `B → C` với nhãn `Có`, `B → D` với nhãn `Không`.
- Dòng Mermaid `click` bị loại như hành vi liên kết/thực thi không thuộc quan hệ quy trình; URL và chỉ dẫn nhúng không được thực thi, không được đưa vào SVG.
- Không truy cập mạng, không tải font/tài nguyên ngoài. SVG tĩnh, tự chứa và dùng font hệ thống.
- Kiểm tra: semantic `PASS`; fidelity `PASS` (7 source record của phần đồ thị); SVG contract `PASS`; geometry `PASS` (4 node, 3 route); kiểm tra trực quan cục bộ hoàn tất.
- Không sửa repository.
